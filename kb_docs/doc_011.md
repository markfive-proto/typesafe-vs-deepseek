# LLM request tracing

Capturing a full span tree for a single request across every step — retrieval, each model call, each tool invocation — so a slow or wrong response can be traced back to exactly which step caused it, the AI-pipeline equivalent of distributed tracing.

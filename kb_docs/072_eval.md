# Toxicity/safety classifiers for eval

Automated classifiers run over model outputs at eval time to flag policy-violating content at scale, usually as a gating check before a model or prompt change can ship, not just a production-time filter.

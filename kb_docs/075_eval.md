# Cost-normalized evaluation

Scoring quality per dollar or per token rather than quality alone, since a marginally better model that costs 10x more is often the wrong production choice even if it wins on a raw accuracy leaderboard.

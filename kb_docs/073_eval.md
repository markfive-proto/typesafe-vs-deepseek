# Consistency/robustness testing

Checking whether small, meaning-preserving changes to an input (rephrasing, typos, reordering) produce the same output — a model whose answer flips on irrelevant wording changes is fragile in a way accuracy-only metrics won't catch.

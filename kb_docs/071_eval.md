# Retrieval quality evaluation

Measuring a RAG system's retrieval step in isolation using precision/recall/NDCG against labeled relevant documents, since a wrong or missing retrieval caps the generation step's quality no matter how good the model is.

# Cost/budget control harness

A layer that tracks token/dollar spend per session or per user and halts or throttles an agent before it runs away — especially important for autonomous loops that could otherwise call tools or the model indefinitely.

# Sandboxed code execution harness

Isolated environments (containers, microVMs) where an agent can run arbitrary code it wrote without risking the host system, usually with resource limits, network restrictions, and a fresh filesystem per run.

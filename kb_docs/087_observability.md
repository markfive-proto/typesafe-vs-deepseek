# User feedback capture loops

Lightweight in-product signals (thumbs up/down, regeneration requests, edits) wired back into the observability pipeline so real user judgment supplements automated metrics, and often becomes new eval or training data.

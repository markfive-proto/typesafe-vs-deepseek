# Structured output enforcement

Harness logic that constrains or validates a model's output against a schema (JSON schema, regex, grammar) so downstream code can rely on the shape of the response instead of parsing free text defensively.

# Code generation correctness eval

Grading generated code by whether it actually passes a held-out test suite (pass@k) rather than by surface similarity to reference code, since two functionally identical solutions can look completely different.

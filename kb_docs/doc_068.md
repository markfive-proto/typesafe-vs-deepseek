# Golden dataset regression suites

A fixed, curated set of input/expected-output pairs run against every model or prompt change to catch regressions, the AI equivalent of a unit test suite, usually the first eval infrastructure a team builds.

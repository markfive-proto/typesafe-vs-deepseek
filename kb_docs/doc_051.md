# Mixed-precision inference

Running different parts of the computation at different numerical precisions (e.g. fp16 activations with int8 weights) to balance speed and accuracy more finely than a single blanket quantization level.

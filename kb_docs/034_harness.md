# Agent-to-agent protocols (MCP-style)

Standardized interfaces (like the Model Context Protocol) that let an agent discover and call tools/resources exposed by another system without bespoke integration code for each one, the same way HTTP standardized web communication.

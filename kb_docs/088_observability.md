# Session replay for agent debugging

Tooling that reconstructs an agent's full decision trace — every tool call, every intermediate thought, every retrieved document — in a reviewable timeline, essential for debugging an agent that went wrong three steps before the visible failure.

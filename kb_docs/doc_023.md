# Plugin/skill systems for agents

A registry pattern where discrete capabilities (a skill, a plugin, an MCP server) can be added to an agent without changing its core loop, letting the same harness pick up new tools without a redeploy.

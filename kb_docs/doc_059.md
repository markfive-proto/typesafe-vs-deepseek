# Model quantization

Reducing a model's numerical precision (fp16 to int8 or int4) to cut memory footprint and increase throughput, trading a small, usually acceptable accuracy loss for meaningfully cheaper and faster inference.

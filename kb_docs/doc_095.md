# Session/state persistence harness

The mechanism for saving and resuming an agent's conversation, tool state, and working files across process restarts, so a long-running task survives a crash or a deliberate pause-and-resume.

# Continuous eval in CI pipelines

Running the eval suite automatically on every prompt, model, or code change as part of CI, blocking a merge if quality drops below a threshold — treating eval scores as a build gate, not a one-off report.

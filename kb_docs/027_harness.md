# Browser automation agent harness

Tooling that gives an agent a controllable browser — click, type, read the DOM or a screenshot — to complete web tasks a plain API can't reach, with the accessibility tree or vision model doing the actual page understanding.

# Prompt chaining pipelines

Harnesses that wire multiple discrete LLM calls into a fixed pipeline (extract, then classify, then generate) rather than one freeform agent loop — more predictable and debuggable than a fully autonomous agent, at the cost of flexibility.

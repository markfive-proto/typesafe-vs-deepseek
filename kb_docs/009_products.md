# AI meeting summarizers

Tools that transcribe a call and extract a structured summary — decisions, action items, owners — rather than a flat transcript. Speaker diarization accuracy and correctly attributing action items to the right person are the recurring failure modes.

# Task queue/scheduler harness

Infrastructure that manages many agent runs concurrently — queuing, prioritizing, retrying, capping concurrency — so a harness built for one interactive session can scale to background batch workloads.

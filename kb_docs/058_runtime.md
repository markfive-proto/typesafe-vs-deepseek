# Model version rollout strategies

Canary or blue-green deployment patterns applied to model upgrades — routing a small percentage of traffic to a new model version and comparing quality/cost metrics before a full cutover, since model behavior changes are harder to unit-test than code.

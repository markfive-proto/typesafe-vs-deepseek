# CLI coding agent harness

A terminal-based orchestration layer (like Claude Code) that gives a model tools — file read/write, shell exec, search — and loops it against a task until done, handling permissions, context management, and session state around the raw model calls.

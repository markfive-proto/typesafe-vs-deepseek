# AI coding assistants

Inline pair-programming tools (Copilot-style) that suggest completions, whole functions, or multi-file edits inside the editor. The best ones use the open files, recent edits, and repo structure as context, and increasingly run in an agentic loop that edits, runs tests, and iterates rather than a single autocomplete suggestion.

# Guardrail violation logging

Recording every time a policy/guardrail layer blocks or modifies an agent's proposed action, both for security review and to tune the guardrail itself when it's blocking too aggressively or missing real violations.

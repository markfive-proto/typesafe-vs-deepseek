# Prompt injection detection monitoring

Runtime scanning of inputs (and sometimes outputs) for patterns consistent with prompt injection attempts, logged and alerted on separately from generic errors since it's a security concern, not just a quality one.

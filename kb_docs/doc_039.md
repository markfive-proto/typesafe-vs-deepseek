# Image generation products

Consumer and API products wrapping diffusion or autoregressive image models, usually with a control layer (reference images, masks, style presets) on top of a raw model to make output predictable enough for commercial use like ad creative or product mockups.

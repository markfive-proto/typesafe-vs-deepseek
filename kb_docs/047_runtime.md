# Model routing / load balancing

Directing each request to a specific model or provider based on cost, latency, capability, or current load — e.g. cheap requests to a small model, hard ones to a frontier model — as a runtime decision, not a build-time choice.

# Automated red-teaming

Systematically generating adversarial inputs (jailbreak attempts, edge cases, prompt injections) to probe a model or agent for failure modes before real users find them, often itself powered by another model generating the attacks.

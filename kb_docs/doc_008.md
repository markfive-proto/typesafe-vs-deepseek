# Prompt/response logging pipelines

Infrastructure that durably records every prompt and completion (with appropriate redaction) for later debugging, eval-set mining, and incident investigation — without this, a production issue is nearly impossible to reproduce after the fact.

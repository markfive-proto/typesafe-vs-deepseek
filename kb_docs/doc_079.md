# Calibration and confidence evaluation

Measuring whether a model's stated confidence (or a system's probability score) actually matches its real accuracy — a model that's right 70% of the time when it claims 70% confidence is well-calibrated, and calibration is what lets downstream code trust a threshold.

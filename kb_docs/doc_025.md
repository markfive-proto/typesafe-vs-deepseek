# LLM-as-judge evaluation

Using a strong model to score another model's outputs against a rubric, instead of exact-match or human grading — scalable and cheap, but only as reliable as the judge model's own biases and blind spots, which need separate calibration.

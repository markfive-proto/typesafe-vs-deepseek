# Testing harness for agent behavior

A framework for running an agent against fixed scenarios and asserting on its actions/outputs, the agent-world equivalent of unit tests, needed because agent behavior can silently regress when the underlying model or prompt changes.

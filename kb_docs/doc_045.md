# Guardrail/policy enforcement layer

A harness component that checks proposed agent actions (a shell command, a file write, a purchase) against a policy before executing them, independent of whether the underlying model 'wants' to do something risky.

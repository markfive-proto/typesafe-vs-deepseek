# Context-window packing strategy

The harness decision of what to include, in what order, and with what priority when the accumulated context (system prompt, tool defs, history, retrieved docs) exceeds the model's window — often the single biggest lever on agent quality.

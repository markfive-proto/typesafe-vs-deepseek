# Synthetic eval data generation

Using a model to generate large volumes of plausible test cases (edge cases, adversarial examples, paraphrases) when hand-labeled eval data is too scarce or expensive to cover the space adequately.

# CI/CD integration harness

The glue that lets an agent trigger builds, read test/lint output, and act on failures as part of a pipeline, turning a chat-style agent into something that participates in existing engineering workflows.

# Root-cause analysis tooling

Purpose-built tooling for diagnosing why a specific agent run failed — replaying the exact context, isolating which step diverged from expected behavior — since generic APM tools weren't built for the shape of agent failures.

# Eval dataset contamination detection

Checking whether benchmark questions leaked into a model's training data, which would make its benchmark score meaningless — an increasingly important check as models are trained on ever-larger, less curated web-scale data.

# Multi-turn conversation evaluation

Assessing quality across an entire conversation rather than a single turn — does the agent remember earlier context, avoid contradicting itself, actually resolve the user's goal — since single-turn metrics miss most real agent failure modes.

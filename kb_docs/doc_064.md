# Drift detection

Monitoring whether the distribution of production inputs or outputs is shifting over time relative to what the system was built and evaluated against — a sign that offline eval numbers no longer reflect current reality.

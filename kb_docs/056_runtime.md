# Fallback/failover across providers

Runtime logic that automatically retries a failed or degraded request against a second model provider, treating model APIs like any other dependency that needs redundancy rather than a single point of failure.

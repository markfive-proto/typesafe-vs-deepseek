# Correlation IDs across services

Attaching a single trace ID to a request as it crosses multiple services (frontend, orchestrator, model provider, tool APIs) so logs from every hop can be joined back into one coherent timeline.
